apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.app.name }}-ssh
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.app.name }}-ssh
  template:
    metadata:
      labels:
        app: {{ .Values.app.name }}-ssh
    spec:
      containers:
      - name: ssh-server
        image: lscr.io/linuxserver/openssh-server:latest
        ports:
        - containerPort: 2222
        env:
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        - name: TZ
          value: "Etc/UTC"
        - name: PUBLIC_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.app.name }}-ssh-key
              key: ssh-key
        - name: USER_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.app.name }}-ssh-key
              key: password
        - name: USER_NAME
          value: "ncote"
        - name: PASSWORD_ACCESS
          value: "true"  # Disable password auth, keys only
        resources:
          limits:
            cpu: 200m
            memory: 128Mi
          requests:
            cpu: 100m
            memory: 64Mi